function findElement(arr, func) {
  let result = false;
  let num=0;
  for(let i=0;i<arr.length;i++)
    {
       result=func(arr[i])
       if(result==true){
       num=arr[i];
       return num;
       }
    }
  return undefined;
}

findElement([1, 2, 3, 4], num => num % 2 === 0);
